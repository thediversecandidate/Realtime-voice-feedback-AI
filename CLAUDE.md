# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

An empty repository — the only file is a broken autogenerated
`README.md` (its `$repo` template variable was never substituted). There is
no source code, dependency manifest, or test setup yet. If asked to build
"the realtime voice feedback AI," this is a from-scratch start, not a
partially-built one.
